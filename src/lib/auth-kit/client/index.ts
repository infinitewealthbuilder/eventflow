/**
 * Auth Kit — Client Exports
 *
 * Usage:
 *   import { AuthProvider, useAuth, useUser, createBrowserClient } from '@/lib/auth-kit/client';
 */

export { AuthProvider, useAuthContext } from './auth-provider';
export { useAuth } from './use-auth';
export { useUser } from './use-user';
export { createBrowserClient } from './browser-client';
